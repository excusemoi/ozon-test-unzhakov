package service

import (
	"testing"
)

func TestLinkService_CreateShortLink(t *testing.T) {
	//ctl := gomock.NewController(t)
	//defer ctl.Finish()
	//storage := mock_storage.NewMockLinkStorage(ctl)
	//service, err := NewLinkService(storage, 10)
	//require.NoError(t, err)
	//
	//cases := struct {
	//	input
	//}{}
}
